version: "3.8"

services:
  signup-app:
    build: .
    container_name: signup-app-latest   # always keep the latest version under this name
    ports:
      - "80:3000"                        # EC2 port 80 -> container port 3000
    restart: always                       # auto-restart on reboot
